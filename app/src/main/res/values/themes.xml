<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.BasicApp.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <!-- Point to our wrapper drawable instead of directly to the PNG -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon_small</item>
        <item name="windowSplashScreenIconBackgroundColor">@android:color/transparent</item>
        <item name="postSplashScreenTheme">@style/Theme.BasicApp</item>
    </style>

    <style name="Theme.BasicApp" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/fairblack</item>
    </style>
</resources>


